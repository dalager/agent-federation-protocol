# Ruflo local secrets and runtime data
.env
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/
.agents/skills/ruflo
.claude
.claude-flow
.swarm
ruvector.db

# Local MCP server registration — tooling config, not project source
.mcp.json

# ADR-0034 Decision 4 — scripts/release-archive.sh's build output
dist/

# Python packaging artifacts of `pip install ./src/verifier` (ADR-0034 Decision 4)
src/verifier/build/
src/verifier/*.egg-info/
